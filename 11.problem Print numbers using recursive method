#include <stdio.h>
void Num(int n)
{
    if (n > 0)
    {
        Num(n - 1);
        printf("%d ", n);
    }
}
int main()
{
    int n;
    scanf("%d", &n);
    Num(n);
    printf("\n");
    return 0;
}
